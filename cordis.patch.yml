# Loader ticket so `dsh plugin add` accepts this package as a DSH bundle.
# The Community v0.15 FacetModule is activated by @dsh-std/adapter-dsh.
- insert:
    - id: dsh-github-installer
      name: dsh-github-installer
